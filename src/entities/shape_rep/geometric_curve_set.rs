//! `GEOMETRIC_CURVE_SET` handler — Pass 6-4f. Hosts the shared body for
//! the `GEOMETRIC_SET` sister (subtype of the same EXPRESS shape; both
//! split items into curves + points). Body wired in Plan 6 stage C5.

// DOMAIN_TBD: catalog group is geometry_3d but dispatch boundary is shape_rep
// (Pass 6-4f). Plan 7+ IR Roadmap 시 재배치 검토.
